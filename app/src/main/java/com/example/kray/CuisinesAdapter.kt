package com.example.kray

import android.view.LayoutInflater
import android.view.View
import android.view.ViewGroup
import androidx.recyclerview.widget.RecyclerView

/*
class CuisinesAdapter : RecyclerView.Adapter<CuisinesAdapter.ViewHolder>() {

    private val mCuisines: List<Cuisines> = listOf()

    override fun onCreateViewHolder(parent: ViewGroup, viewType: Int): ViewHolder {
        val view = LayoutInflater.from(parent.context).inflate(R.layout.uncheck_item, parent, false)

        return ViewHolder(view)
    }

    override fun getItemCount(): Int = mCuisines.count()

    class ViewHolder(view: View) : RecyclerView.ViewHolder(view) {

    }
}*/
